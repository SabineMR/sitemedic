diff --git a/node_modules/expo-router/build/ExpoRoot.js b/node_modules/expo-router/build/ExpoRoot.js
index 4bef114..35b0c21 100644
--- a/node_modules/expo-router/build/ExpoRoot.js
+++ b/node_modules/expo-router/build/ExpoRoot.js
@@ -53,12 +53,14 @@ const Sitemap_1 = require("./views/Sitemap");
 const SplashScreen = __importStar(require("./views/Splash"));
 const Unmatched_1 = require("./views/Unmatched");
 const isTestEnv = process.env.NODE_ENV === 'test';
-const INITIAL_METRICS = react_native_1.Platform.OS === 'web' || isTestEnv
-    ? {
-        frame: { x: 0, y: 0, width: 0, height: 0 },
-        insets: { top: 0, left: 0, right: 0, bottom: 0 },
-    }
-    : undefined;
+// PATCH: Always provide initialMetrics so SafeAreaProvider renders children immediately.
+// On native (iOS/Android) with New Architecture, NativeSafeAreaProvider may not fire
+// onInsetsChange in time, leaving insets=null and children=null (blank screen).
+// SafeAreaProvider will still receive real insets from the native module once it fires.
+const INITIAL_METRICS = {
+    frame: { x: 0, y: 0, width: 0, height: 0 },
+    insets: { top: 0, left: 0, right: 0, bottom: 0 },
+};
 const documentTitle = {
     enabled: false,
 };
diff --git a/node_modules/expo-router/build/fork/useLinking.native.js b/node_modules/expo-router/build/fork/useLinking.native.js
index 23fff02..0027570 100644
--- a/node_modules/expo-router/build/fork/useLinking.native.js
+++ b/node_modules/expo-router/build/fork/useLinking.native.js
@@ -187,7 +187,18 @@ function getInitialURLWithTimeout() {
     }
     else if (react_native_1.Platform.OS === 'ios') {
         // Use the new Expo API for iOS. This has better support for App Clips and handoff.
-        return ExpoLinking.getLinkingURL();
+        // WORKAROUND: In React Native New Architecture (Bridgeless/Fabric), Turbo Module methods
+        // may return Promises even when typed as synchronous. Wrap with a timeout to prevent
+        // NavigationContainer from waiting forever (which causes a blank screen).
+        const result = ExpoLinking.getLinkingURL();
+        if (result && typeof result.then === 'function') {
+            // It returned a Promise — race against a 150ms timeout
+            return Promise.race([
+                result,
+                new Promise((resolve) => setTimeout(() => resolve(null), 150)),
+            ]);
+        }
+        return result;
     }
     return Promise.race([
         // TODO: Phase this out in favor of expo-linking on Android.
