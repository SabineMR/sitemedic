# Supabase Configuration
# Copy this file to .env.local and fill in your Supabase project details

# Your Supabase project URL (found in Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anon/public API key (found in Project Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Google Places API Key (for address autocomplete in quote builder)
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your-google-places-api-key

# Stripe API Keys (for payment processing)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key
STRIPE_SECRET_KEY=your-stripe-secret-key

# High-risk and low-risk worker ratios (for paramedic recommendations)
NEXT_PUBLIC_HIGH_RISK_RATIO=50
NEXT_PUBLIC_LOW_RISK_RATIO=100

# Public site URL (used in transactional emails for dashboard links)
# In development this defaults to http://localhost:30500
NEXT_PUBLIC_SITE_URL=https://sitemedic.co.uk

# Admin notification email — receives quote requests and contact form enquiries
# Defaults to admin@sitemedic.co.uk if not set
ADMIN_EMAIL=admin@sitemedic.co.uk

# Revenue split defaults (can be overridden per-medic in the medics table)
# Platform takes DEFAULT_PLATFORM_FEE_PERCENT, medic receives DEFAULT_MEDIC_PAYOUT_PERCENT
# Must sum to 100
NEXT_PUBLIC_DEFAULT_PLATFORM_FEE_PERCENT=60
NEXT_PUBLIC_DEFAULT_MEDIC_PAYOUT_PERCENT=40

# Referral payout — percentage of booking total paid to the referring party
# Deducted from the platform's share (does NOT affect medic payout)
NEXT_PUBLIC_REFERRAL_PAYOUT_PERCENT=10
