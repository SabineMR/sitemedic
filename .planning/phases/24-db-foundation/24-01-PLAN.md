---
phase: 24-db-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/package.json
autonomous: true

must_haves:
  truths:
    - "Next.js package.json specifier requires >=15.2.3 (CVE-2025-29927 patch)"
    - "eslint-config-next specifier matches the next specifier"
    - "pnpm install resolves without errors"
    - "pnpm build completes with zero type errors"
  artifacts:
    - path: "web/package.json"
      provides: "Updated next and eslint-config-next version specifiers"
      contains: "15.2.3"
  key_links:
    - from: "web/package.json"
      to: "pnpm-lock.yaml"
      via: "pnpm install resolving dependencies"
      pattern: "next.*15\\."
---

<objective>
Bump the Next.js version specifier in web/package.json to >=15.2.3 to patch CVE-2025-29927 (middleware bypass). Also bump eslint-config-next to match.

Purpose: The CVE-2025-29927 middleware bypass must be patched BEFORE Phase 26 expands the middleware attack surface with subdomain routing. Research confirmed Next.js 15.5.12 is already installed in node_modules via the pnpm lock file, so this is a specifier-only update -- no actual version change.

Output: Updated web/package.json with explicit >=15.2.3 floor on both next and eslint-config-next.
</objective>

<execution_context>
@/Users/sabineresoagli/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sabineresoagli/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Next.js and eslint-config-next version specifiers</name>
  <files>web/package.json</files>
  <action>
  In web/package.json:
  1. Change `"next": "^15.1.5"` to `"next": "^15.2.3"` (dependencies section, line 46)
  2. Change `"eslint-config-next": "^15.1.5"` to `"eslint-config-next": "^15.2.3"` (devDependencies section, line 72)

  Then run from the repo root:
  ```
  cd /Users/sabineresoagli/GitHub/sitemedic && pnpm install
  ```

  This should resolve quickly since 15.5.12 is already in node_modules. The lock file may update the specifier constraint but the resolved version stays the same.
  </action>
  <verify>
  Run these commands from /Users/sabineresoagli/GitHub/sitemedic/web:
  1. `grep '"next"' package.json` -- must show `"^15.2.3"`
  2. `grep 'eslint-config-next' package.json` -- must show `"^15.2.3"`
  3. `cd /Users/sabineresoagli/GitHub/sitemedic && pnpm build --filter=sitemedic-web` -- must exit 0 with no type errors
  </verify>
  <done>
  web/package.json has `"next": "^15.2.3"` and `"eslint-config-next": "^15.2.3"`. pnpm install succeeds. pnpm build passes with no type errors. CVE-2025-29927 specifier floor is in place.
  </done>
</task>

</tasks>

<verification>
- `grep '"next"' /Users/sabineresoagli/GitHub/sitemedic/web/package.json` shows `"^15.2.3"`
- `grep 'eslint-config-next' /Users/sabineresoagli/GitHub/sitemedic/web/package.json` shows `"^15.2.3"`
- `cd /Users/sabineresoagli/GitHub/sitemedic && pnpm build --filter=sitemedic-web` exits 0
</verification>

<success_criteria>
Next.js version specifier in package.json explicitly requires >=15.2.3 (CVE-2025-29927 patch). eslint-config-next matches. Build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/24-db-foundation/24-01-SUMMARY.md`
</output>
